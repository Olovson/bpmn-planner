# Codex Batch Override Generation - Alla Filer

**Prompt-versioner:**
- feature-epic: 1.0.0
- business-rule: 1.0.0

När du genererar innehåll, lägg till denna kommentar i varje fil:
```typescript
/**
 * PROMPT VERSION: 1.0.0 (eller 1.0.0 för business-rule)
 * Genererad: [datum]
 */
```

Detta dokument innehåller instruktioner för att batch-generera innehåll för 94 override-filer.

## ⚠️ KRITISKT - Läs detta först!

### Regel 1: Skriv ALDRIG över befintligt innehåll!
- **Kontrollera FÖRST** om ett fält redan har innehåll (inte 'TODO')
- Om fältet HAR innehåll → LÄMNA DET ORÖRT, gå vidare till nästa fält
- Ersätt BARA fält som är:
  - 'TODO' (exakt strängen 'TODO')
  - Tomma arrayer: []
  - Tomma strängar: ''
- **Om du ser innehåll som INTE är 'TODO' → RÖR INTE DET!**

### Regel 2: Automatisk bearbetning
- Bearbeta ALLA filer automatiskt utan att fråga
- Fortsätt från fil 1 till fil 94 utan att stoppa
- Fråga INTE om du ska fortsätta - bara fortsätt
- Bearbeta filerna en i taget, men kontinuerligt
- När en fil är klar, gå direkt vidare till nästa

### Regel 3: Statusrapportering (VIKTIGT!)
- Efter varje fil du bearbetar, uppdatera statusfilen: `.codex-batch-status.json`
- Format:
```json
{
  "total": 94,
  "completed": ["fil1", "fil2", ...],
  "current": "filX",
  "lastUpdated": "2024-11-26T20:00:00Z"
}
```
- Lägg till varje klar fil i `completed`-arrayen (om den inte redan finns)
- Uppdatera `current` med filen du just bearbetar (eller null om klar)
- Uppdatera `lastUpdated` med aktuellt datum/tid (ISO-format)
- **Rapportera INTE i chatten - bara uppdatera filen!**
- **Fråga INTE om du ska fortsätta - bara uppdatera status och fortsätt!**

### Regel 3: Statusrapportering (VIKTIGT!)
- Efter varje fil du bearbetar, uppdatera statusfilen: `.codex-batch-status.json`
- Format:
```json
{
  "total": 94,
  "completed": [fil1, fil2, ...],
  "current": "filX",
  "lastUpdated": "2024-11-26T20:00:00Z"
}
```
- Lägg till varje klar fil i `completed`-arrayen
- Uppdatera `current` med filen du just bearbetar
- Uppdatera `lastUpdated` med aktuellt datum/tid
- **Rapportera INTE i chatten - bara uppdatera filen!**
- **Fråga INTE om du ska fortsätta - bara uppdatera status och fortsätt!**

## Exempel på korrekt beteende

**FÖRE (filen har redan innehåll):**
```typescript
export const overrides: FeatureGoalDocOverrides = {
  summary: 'Detta Feature Goal möjliggör...',  // ← HAR INNEHÅLL, LÄMNA ORÖRT!
  effectGoals: ['TODO'],  // ← ÄR 'TODO', ERSAETT!
  scopeIncluded: ['Ingår: Digital ansökan'],  // ← HAR INNEHÅLL, LÄMNA ORÖRT!
};
```

**EFTER (bara TODO ersätts):**
```typescript
export const overrides: FeatureGoalDocOverrides = {
  summary: 'Detta Feature Goal möjliggör...',  // ← OFÖRÄNDRAT (hade innehåll)
  effectGoals: ['Automatisera manuellt arbete', 'Förbättra kreditbedömningar'],  // ← ERSAETT (var 'TODO')
  scopeIncluded: ['Ingår: Digital ansökan'],  // ← OFÖRÄNDRAT (hade innehåll)
};
```

## Workflow

Bearbeta filerna en i taget, automatiskt och kontinuerligt. För varje fil:

1. **Öppna filen** och läs NODE CONTEXT-kommentaren (överst i filen)
2. **Hämta rätt prompt** baserat på type i NODE CONTEXT:
   - `feature-goal` eller `epic` → Läs `prompts/llm/feature_epic_prompt.md`
   - `business-rule` → Läs `prompts/llm/dmn_businessrule_prompt.md`
3. **Generera JSON** enligt promptens instruktioner (svenska, formell bankton)
4. **Uppdatera BARA** fälten som listas nedan för varje fil
5. **Behåll** allt annat innehåll oförändrat
6. **Spara** filen och gå vidare till nästa

## Filer att bearbeta


### 1. src/data/node-docs/business-rule/mortgage-se-credit-decision.evaluate-credit-decision-rules.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-credit-decision.bpmn
- elementId: evaluate-credit-decision-rules
- type: business-rule

**Prompt att använda:**
- prompts/llm/dmn_businessrule_prompt.md

**Fält som behöver uppdateras:**



**Instruktioner:**
1. Öppna filen: `src/data/node-docs/business-rule/mortgage-se-credit-decision.evaluate-credit-decision-rules.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/dmn_businessrule_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** 
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/business-rule/mortgage-se-credit-decision.evaluate-credit-decision-rules.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 2. src/data/node-docs/business-rule/mortgage-se-credit-evaluation.evaluate-amortisation.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-credit-evaluation.bpmn
- elementId: evaluate-amortisation
- type: business-rule

**Prompt att använda:**
- prompts/llm/dmn_businessrule_prompt.md

**Fält som behöver uppdateras:**



**Instruktioner:**
1. Öppna filen: `src/data/node-docs/business-rule/mortgage-se-credit-evaluation.evaluate-amortisation.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/dmn_businessrule_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** 
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/business-rule/mortgage-se-credit-evaluation.evaluate-amortisation.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 3. src/data/node-docs/business-rule/mortgage-se-credit-evaluation.evaluate-application.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-credit-evaluation.bpmn
- elementId: evaluate-application
- type: business-rule

**Prompt att använda:**
- prompts/llm/dmn_businessrule_prompt.md

**Fält som behöver uppdateras:**



**Instruktioner:**
1. Öppna filen: `src/data/node-docs/business-rule/mortgage-se-credit-evaluation.evaluate-application.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/dmn_businessrule_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** 
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/business-rule/mortgage-se-credit-evaluation.evaluate-application.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 4. src/data/node-docs/business-rule/mortgage-se-credit-evaluation.evaluate-credit-policies.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-credit-evaluation.bpmn
- elementId: evaluate-credit-policies
- type: business-rule

**Prompt att använda:**
- prompts/llm/dmn_businessrule_prompt.md

**Fält som behöver uppdateras:**



**Instruktioner:**
1. Öppna filen: `src/data/node-docs/business-rule/mortgage-se-credit-evaluation.evaluate-credit-policies.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/dmn_businessrule_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** 
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/business-rule/mortgage-se-credit-evaluation.evaluate-credit-policies.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 5. src/data/node-docs/business-rule/mortgage-se-credit-evaluation.evaluate-household.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-credit-evaluation.bpmn
- elementId: evaluate-household
- type: business-rule

**Prompt att använda:**
- prompts/llm/dmn_businessrule_prompt.md

**Fält som behöver uppdateras:**



**Instruktioner:**
1. Öppna filen: `src/data/node-docs/business-rule/mortgage-se-credit-evaluation.evaluate-household.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/dmn_businessrule_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** 
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/business-rule/mortgage-se-credit-evaluation.evaluate-household.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 6. src/data/node-docs/business-rule/mortgage-se-credit-evaluation.evaluate-stakeholders.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-credit-evaluation.bpmn
- elementId: evaluate-stakeholders
- type: business-rule

**Prompt att använda:**
- prompts/llm/dmn_businessrule_prompt.md

**Fält som behöver uppdateras:**



**Instruktioner:**
1. Öppna filen: `src/data/node-docs/business-rule/mortgage-se-credit-evaluation.evaluate-stakeholders.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/dmn_businessrule_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** 
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/business-rule/mortgage-se-credit-evaluation.evaluate-stakeholders.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 7. src/data/node-docs/business-rule/mortgage-se-credit-evaluation.fetch-product.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-credit-evaluation.bpmn
- elementId: fetch-product
- type: business-rule

**Prompt att använda:**
- prompts/llm/dmn_businessrule_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/business-rule/mortgage-se-credit-evaluation.fetch-product.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/dmn_businessrule_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/business-rule/mortgage-se-credit-evaluation.fetch-product.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 8. src/data/node-docs/business-rule/mortgage-se-disbursement.evaluate-disbursement-rules.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-disbursement.bpmn
- elementId: evaluate-disbursement-rules
- type: business-rule

**Prompt att använda:**
- prompts/llm/dmn_businessrule_prompt.md

**Fält som behöver uppdateras:**



**Instruktioner:**
1. Öppna filen: `src/data/node-docs/business-rule/mortgage-se-disbursement.evaluate-disbursement-rules.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/dmn_businessrule_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** 
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/business-rule/mortgage-se-disbursement.evaluate-disbursement-rules.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 9. src/data/node-docs/business-rule/mortgage-se-document-generation.select-documents.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-document-generation.bpmn
- elementId: select-documents
- type: business-rule

**Prompt att använda:**
- prompts/llm/dmn_businessrule_prompt.md

**Fält som behöver uppdateras:**



**Instruktioner:**
1. Öppna filen: `src/data/node-docs/business-rule/mortgage-se-document-generation.select-documents.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/dmn_businessrule_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** 
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/business-rule/mortgage-se-document-generation.select-documents.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 10. src/data/node-docs/business-rule/mortgage-se-internal-data-gathering.pre-screen-party.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-internal-data-gathering.bpmn
- elementId: pre-screen-party
- type: business-rule

**Prompt att använda:**
- prompts/llm/dmn_businessrule_prompt.md

**Fält som behöver uppdateras:**



**Instruktioner:**
1. Öppna filen: `src/data/node-docs/business-rule/mortgage-se-internal-data-gathering.pre-screen-party.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/dmn_businessrule_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** 
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/business-rule/mortgage-se-internal-data-gathering.pre-screen-party.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 11. src/data/node-docs/business-rule/mortgage-se-kyc.assess-kyc-aml.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-kyc.bpmn
- elementId: assess-kyc-aml
- type: business-rule

**Prompt att använda:**
- prompts/llm/dmn_businessrule_prompt.md

**Fält som behöver uppdateras:**



**Instruktioner:**
1. Öppna filen: `src/data/node-docs/business-rule/mortgage-se-kyc.assess-kyc-aml.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/dmn_businessrule_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** 
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/business-rule/mortgage-se-kyc.assess-kyc-aml.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 12. src/data/node-docs/business-rule/mortgage-se-manual-credit-evaluation.evaluate-requirements.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-manual-credit-evaluation.bpmn
- elementId: evaluate-requirements
- type: business-rule

**Prompt att använda:**
- prompts/llm/dmn_businessrule_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/business-rule/mortgage-se-manual-credit-evaluation.evaluate-requirements.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/dmn_businessrule_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/business-rule/mortgage-se-manual-credit-evaluation.evaluate-requirements.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 13. src/data/node-docs/business-rule/mortgage-se-object-information.evaluate-bostadsratt.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-object-information.bpmn
- elementId: evaluate-bostadsratt
- type: business-rule

**Prompt att använda:**
- prompts/llm/dmn_businessrule_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/business-rule/mortgage-se-object-information.evaluate-bostadsratt.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/dmn_businessrule_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/business-rule/mortgage-se-object-information.evaluate-bostadsratt.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 14. src/data/node-docs/business-rule/mortgage-se-object-information.evaluate-fastighet.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-object-information.bpmn
- elementId: evaluate-fastighet
- type: business-rule

**Prompt att använda:**
- prompts/llm/dmn_businessrule_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/business-rule/mortgage-se-object-information.evaluate-fastighet.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/dmn_businessrule_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/business-rule/mortgage-se-object-information.evaluate-fastighet.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 15. src/data/node-docs/business-rule/mortgage-se-stakeholder.assess-stakeholder.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-stakeholder.bpmn
- elementId: assess-stakeholder
- type: business-rule

**Prompt att använda:**
- prompts/llm/dmn_businessrule_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/business-rule/mortgage-se-stakeholder.assess-stakeholder.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/dmn_businessrule_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/business-rule/mortgage-se-stakeholder.assess-stakeholder.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 16. src/data/node-docs/business-rule/mortgage-se-stakeholder.evaluate-personal-information.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-stakeholder.bpmn
- elementId: evaluate-personal-information
- type: business-rule

**Prompt att använda:**
- prompts/llm/dmn_businessrule_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/business-rule/mortgage-se-stakeholder.evaluate-personal-information.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/dmn_businessrule_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/business-rule/mortgage-se-stakeholder.evaluate-personal-information.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 17. src/data/node-docs/epic/mortgage-se-appeal.screen-appeal.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-appeal.bpmn
- elementId: screen-appeal
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-appeal.screen-appeal.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-appeal.screen-appeal.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 18. src/data/node-docs/epic/mortgage-se-appeal.submit-appeal.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-appeal.bpmn
- elementId: submit-appeal
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-appeal.submit-appeal.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-appeal.submit-appeal.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 19. src/data/node-docs/epic/mortgage-se-application.confirm-application.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-application.bpmn
- elementId: confirm-application
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-application.confirm-application.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-application.confirm-application.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 20. src/data/node-docs/epic/mortgage-se-collateral-registration.distribute-ansokan-till-inskrivningsmyndigheten.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-collateral-registration.bpmn
- elementId: distribute-ansokan-till-inskrivningsmyndigheten
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-collateral-registration.distribute-ansokan-till-inskrivningsmyndigheten.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-collateral-registration.distribute-ansokan-till-inskrivningsmyndigheten.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 21. src/data/node-docs/epic/mortgage-se-collateral-registration.distribute-notice-of-pledge-to-brf.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-collateral-registration.bpmn
- elementId: distribute-notice-of-pledge-to-brf
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-collateral-registration.distribute-notice-of-pledge-to-brf.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-collateral-registration.distribute-notice-of-pledge-to-brf.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 22. src/data/node-docs/epic/mortgage-se-collateral-registration.verify.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-collateral-registration.bpmn
- elementId: verify
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-collateral-registration.verify.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-collateral-registration.verify.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 23. src/data/node-docs/epic/mortgage-se-credit-decision.evaluate-application-board.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-credit-decision.bpmn
- elementId: evaluate-application-board
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-credit-decision.evaluate-application-board.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-credit-decision.evaluate-application-board.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 24. src/data/node-docs/epic/mortgage-se-credit-decision.evaluate-application-committee.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-credit-decision.bpmn
- elementId: evaluate-application-committee
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-credit-decision.evaluate-application-committee.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-credit-decision.evaluate-application-committee.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 25. src/data/node-docs/epic/mortgage-se-credit-decision.evaluate-application-four-eyes.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-credit-decision.bpmn
- elementId: evaluate-application-four-eyes
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-credit-decision.evaluate-application-four-eyes.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-credit-decision.evaluate-application-four-eyes.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 26. src/data/node-docs/epic/mortgage-se-credit-evaluation.calculate-household-affordability.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-credit-evaluation.bpmn
- elementId: calculate-household-affordability
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-credit-evaluation.calculate-household-affordability.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-credit-evaluation.calculate-household-affordability.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 27. src/data/node-docs/epic/mortgage-se-credit-evaluation.fetch-price.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-credit-evaluation.bpmn
- elementId: fetch-price
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-credit-evaluation.fetch-price.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-credit-evaluation.fetch-price.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 28. src/data/node-docs/epic/mortgage-se-credit-evaluation.fetch-risk-classification.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-credit-evaluation.bpmn
- elementId: fetch-risk-classification
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-credit-evaluation.fetch-risk-classification.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-credit-evaluation.fetch-risk-classification.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 29. src/data/node-docs/epic/mortgage-se-disbursement.archive-documents.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-disbursement.bpmn
- elementId: archive-documents
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-disbursement.archive-documents.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-disbursement.archive-documents.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 30. src/data/node-docs/epic/mortgage-se-disbursement.control-purchase-disbursement-requirements.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-disbursement.bpmn
- elementId: control-purchase-disbursement-requirements
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-disbursement.control-purchase-disbursement-requirements.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-disbursement.control-purchase-disbursement-requirements.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 31. src/data/node-docs/epic/mortgage-se-disbursement.disburse-loan.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-disbursement.bpmn
- elementId: disburse-loan
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-disbursement.disburse-loan.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-disbursement.disburse-loan.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 32. src/data/node-docs/epic/mortgage-se-disbursement.disbursement-control.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-disbursement.bpmn
- elementId: disbursement-control
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-disbursement.disbursement-control.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-disbursement.disbursement-control.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 33. src/data/node-docs/epic/mortgage-se-disbursement.verify-disbursement-details.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-disbursement.bpmn
- elementId: verify-disbursement-details
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-disbursement.verify-disbursement-details.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-disbursement.verify-disbursement-details.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 34. src/data/node-docs/epic/mortgage-se-document-generation.generate-documents.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-document-generation.bpmn
- elementId: generate-documents
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-document-generation.generate-documents.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-document-generation.generate-documents.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 35. src/data/node-docs/epic/mortgage-se-documentation-assessment.analyse-documentation.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-documentation-assessment.bpmn
- elementId: analyse-documentation
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-documentation-assessment.analyse-documentation.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-documentation-assessment.analyse-documentation.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 36. src/data/node-docs/epic/mortgage-se-documentation-assessment.assess-documentation.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-documentation-assessment.bpmn
- elementId: assess-documentation
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-documentation-assessment.assess-documentation.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-documentation-assessment.assess-documentation.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 37. src/data/node-docs/epic/mortgage-se-household.register-household-economy-information.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-household.bpmn
- elementId: register-household-economy-information
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-household.register-household-economy-information.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-household.register-household-economy-information.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 38. src/data/node-docs/epic/mortgage-se-internal-data-gathering.fetch-engagements.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-internal-data-gathering.bpmn
- elementId: fetch-engagements
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-internal-data-gathering.fetch-engagements.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-internal-data-gathering.fetch-engagements.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 39. src/data/node-docs/epic/mortgage-se-internal-data-gathering.fetch-party-information.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-internal-data-gathering.bpmn
- elementId: fetch-party-information
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-internal-data-gathering.fetch-party-information.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-internal-data-gathering.fetch-party-information.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 40. src/data/node-docs/epic/mortgage-se-kyc.fetch-aml-kyc-risk.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-kyc.bpmn
- elementId: fetch-aml-kyc-risk
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-kyc.fetch-aml-kyc-risk.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-kyc.fetch-aml-kyc-risk.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 41. src/data/node-docs/epic/mortgage-se-kyc.fetch-screening-and-sanctions.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-kyc.bpmn
- elementId: fetch-screening-and-sanctions
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-kyc.fetch-screening-and-sanctions.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-kyc.fetch-screening-and-sanctions.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 42. src/data/node-docs/epic/mortgage-se-kyc.review-kyc.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-kyc.bpmn
- elementId: review-kyc
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-kyc.review-kyc.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-kyc.review-kyc.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 43. src/data/node-docs/epic/mortgage-se-kyc.submit-self-declaration.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-kyc.bpmn
- elementId: submit-self-declaration
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-kyc.submit-self-declaration.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-kyc.submit-self-declaration.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 44. src/data/node-docs/epic/mortgage-se-manual-credit-evaluation.advanced-underwriting.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-manual-credit-evaluation.bpmn
- elementId: advanced-underwriting
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-manual-credit-evaluation.advanced-underwriting.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-manual-credit-evaluation.advanced-underwriting.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 45. src/data/node-docs/epic/mortgage-se-manual-credit-evaluation.decide-object-value.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-manual-credit-evaluation.bpmn
- elementId: decide-object-value
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-manual-credit-evaluation.decide-object-value.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-manual-credit-evaluation.decide-object-value.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 46. src/data/node-docs/epic/mortgage-se-manual-credit-evaluation.upload-documentation.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-manual-credit-evaluation.bpmn
- elementId: upload-documentation
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-manual-credit-evaluation.upload-documentation.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-manual-credit-evaluation.upload-documentation.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 47. src/data/node-docs/epic/mortgage-se-mortgage-commitment.decide-mortgage-commitment.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-mortgage-commitment.bpmn
- elementId: decide-mortgage-commitment
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-mortgage-commitment.decide-mortgage-commitment.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-mortgage-commitment.decide-mortgage-commitment.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 48. src/data/node-docs/epic/mortgage-se-object-information.fetch-bostadsratts-information.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-object-information.bpmn
- elementId: fetch-bostadsratts-information
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-object-information.fetch-bostadsratts-information.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-object-information.fetch-bostadsratts-information.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 49. src/data/node-docs/epic/mortgage-se-object-information.fetch-brf-information.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-object-information.bpmn
- elementId: fetch-brf-information
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-object-information.fetch-brf-information.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-object-information.fetch-brf-information.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 50. src/data/node-docs/epic/mortgage-se-object-information.fetch-fastighets-information.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-object-information.bpmn
- elementId: fetch-fastighets-information
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-object-information.fetch-fastighets-information.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-object-information.fetch-fastighets-information.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 51. src/data/node-docs/epic/mortgage-se-object.register-loan-details.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-object.bpmn
- elementId: register-loan-details
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-object.register-loan-details.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-object.register-loan-details.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 52. src/data/node-docs/epic/mortgage-se-object.register-source-of-equity.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-object.bpmn
- elementId: register-source-of-equity
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-object.register-source-of-equity.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-object.register-source-of-equity.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 53. src/data/node-docs/epic/mortgage-se-object.valuate-property.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-object.bpmn
- elementId: valuate-property
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-object.valuate-property.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-object.valuate-property.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 54. src/data/node-docs/epic/mortgage-se-offer.advanced-underwriting.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-offer.bpmn
- elementId: advanced-underwriting
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-offer.advanced-underwriting.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-offer.advanced-underwriting.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 55. src/data/node-docs/epic/mortgage-se-offer.decide-offer.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-offer.bpmn
- elementId: decide-offer
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-offer.decide-offer.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-offer.decide-offer.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 56. src/data/node-docs/epic/mortgage-se-signing.create-signing-order.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-signing.bpmn
- elementId: create-signing-order
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-signing.create-signing-order.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-signing.create-signing-order.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 57. src/data/node-docs/epic/mortgage-se-signing.distribute-manual-documents.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-signing.bpmn
- elementId: distribute-manual-documents
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-signing.distribute-manual-documents.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-signing.distribute-manual-documents.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 58. src/data/node-docs/epic/mortgage-se-signing.store-signed-document.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-signing.bpmn
- elementId: store-signed-document
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-signing.store-signed-document.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-signing.store-signed-document.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 59. src/data/node-docs/epic/mortgage-se-signing.upload-document.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-signing.bpmn
- elementId: upload-document
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-signing.upload-document.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-signing.upload-document.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 60. src/data/node-docs/epic/mortgage-se-signing.upload-manual-document.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-signing.bpmn
- elementId: upload-manual-document
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-signing.upload-manual-document.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-signing.upload-manual-document.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 61. src/data/node-docs/epic/mortgage-se-simple-subprocess.UserTask_Sub.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-simple-subprocess.bpmn
- elementId: UserTask_Sub
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-simple-subprocess.UserTask_Sub.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-simple-subprocess.UserTask_Sub.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 62. src/data/node-docs/epic/mortgage-se-stakeholder.consent-to-credit-check.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-stakeholder.bpmn
- elementId: consent-to-credit-check
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-stakeholder.consent-to-credit-check.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-stakeholder.consent-to-credit-check.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 63. src/data/node-docs/epic/mortgage-se-stakeholder.fetch-credit-information.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-stakeholder.bpmn
- elementId: fetch-credit-information
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-stakeholder.fetch-credit-information.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-stakeholder.fetch-credit-information.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 64. src/data/node-docs/epic/mortgage-se-stakeholder.fetch-personal-information.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-stakeholder.bpmn
- elementId: fetch-personal-information
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-stakeholder.fetch-personal-information.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-stakeholder.fetch-personal-information.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 65. src/data/node-docs/epic/mortgage-se-stakeholder.register-personal-economy-information.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-stakeholder.bpmn
- elementId: register-personal-economy-information
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/mortgage-se-stakeholder.register-personal-economy-information.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/mortgage-se-stakeholder.register-personal-economy-information.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 66. src/data/node-docs/epic/simple-process.UserTask_1.doc.ts

**NODE CONTEXT:**
- bpmnFile: simple-process.bpmn
- elementId: UserTask_1
- type: epic

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/epic/simple-process.UserTask_1.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/epic/simple-process.UserTask_1.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 67. src/data/node-docs/feature-goal/mortgage-se-application.household.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-application.bpmn
- elementId: household
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage-se-application.household.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage-se-application.household.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 68. src/data/node-docs/feature-goal/mortgage-se-application.internal-data-gathering.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-application.bpmn
- elementId: internal-data-gathering
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage-se-application.internal-data-gathering.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage-se-application.internal-data-gathering.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 69. src/data/node-docs/feature-goal/mortgage-se-application.object.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-application.bpmn
- elementId: object
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage-se-application.object.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage-se-application.object.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 70. src/data/node-docs/feature-goal/mortgage-se-application.stakeholder.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-application.bpmn
- elementId: stakeholder
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage-se-application.stakeholder.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage-se-application.stakeholder.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 71. src/data/node-docs/feature-goal/mortgage-se-manual-credit-evaluation.credit-evaluation.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-manual-credit-evaluation.bpmn
- elementId: credit-evaluation
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage-se-manual-credit-evaluation.credit-evaluation.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage-se-manual-credit-evaluation.credit-evaluation.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 72. src/data/node-docs/feature-goal/mortgage-se-manual-credit-evaluation.documentation-assessment.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-manual-credit-evaluation.bpmn
- elementId: documentation-assessment
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage-se-manual-credit-evaluation.documentation-assessment.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage-se-manual-credit-evaluation.documentation-assessment.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 73. src/data/node-docs/feature-goal/mortgage-se-mortgage-commitment.credit-evaluation-1.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-mortgage-commitment.bpmn
- elementId: credit-evaluation-1
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage-se-mortgage-commitment.credit-evaluation-1.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage-se-mortgage-commitment.credit-evaluation-1.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 74. src/data/node-docs/feature-goal/mortgage-se-mortgage-commitment.credit-evaluation-2.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-mortgage-commitment.bpmn
- elementId: credit-evaluation-2
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage-se-mortgage-commitment.credit-evaluation-2.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage-se-mortgage-commitment.credit-evaluation-2.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 75. src/data/node-docs/feature-goal/mortgage-se-mortgage-commitment.documentation-assessment.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-mortgage-commitment.bpmn
- elementId: documentation-assessment
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage-se-mortgage-commitment.documentation-assessment.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage-se-mortgage-commitment.documentation-assessment.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 76. src/data/node-docs/feature-goal/mortgage-se-mortgage-commitment.object-information.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-mortgage-commitment.bpmn
- elementId: object-information
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage-se-mortgage-commitment.object-information.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage-se-mortgage-commitment.object-information.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 77. src/data/node-docs/feature-goal/mortgage-se-object.object-information.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-object.bpmn
- elementId: object-information
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage-se-object.object-information.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage-se-object.object-information.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 78. src/data/node-docs/feature-goal/mortgage-se-offer.credit-decision.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage-se-offer.bpmn
- elementId: credit-decision
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage-se-offer.credit-decision.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage-se-offer.credit-decision.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 79. src/data/node-docs/feature-goal/mortgage.appeal.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage.bpmn
- elementId: appeal
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage.appeal.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage.appeal.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 80. src/data/node-docs/feature-goal/mortgage.collateral-registration.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage.bpmn
- elementId: collateral-registration
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage.collateral-registration.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage.collateral-registration.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 81. src/data/node-docs/feature-goal/mortgage.credit-decision.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage.bpmn
- elementId: credit-decision
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage.credit-decision.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage.credit-decision.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 82. src/data/node-docs/feature-goal/mortgage.credit-evaluation.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage.bpmn
- elementId: credit-evaluation
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage.credit-evaluation.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage.credit-evaluation.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 83. src/data/node-docs/feature-goal/mortgage.disbursement-advance.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage.bpmn
- elementId: disbursement-advance
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage.disbursement-advance.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage.disbursement-advance.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 84. src/data/node-docs/feature-goal/mortgage.disbursement.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage.bpmn
- elementId: disbursement
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage.disbursement.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage.disbursement.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 85. src/data/node-docs/feature-goal/mortgage.document-generation-advance.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage.bpmn
- elementId: document-generation-advance
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage.document-generation-advance.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage.document-generation-advance.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 86. src/data/node-docs/feature-goal/mortgage.document-generation.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage.bpmn
- elementId: document-generation
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage.document-generation.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage.document-generation.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 87. src/data/node-docs/feature-goal/mortgage.kyc.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage.bpmn
- elementId: kyc
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage.kyc.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage.kyc.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 88. src/data/node-docs/feature-goal/mortgage.manual-credit-evaluation.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage.bpmn
- elementId: manual-credit-evaluation
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage.manual-credit-evaluation.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage.manual-credit-evaluation.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 89. src/data/node-docs/feature-goal/mortgage.mortgage-commitment.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage.bpmn
- elementId: mortgage-commitment
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage.mortgage-commitment.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage.mortgage-commitment.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 90. src/data/node-docs/feature-goal/mortgage.offer.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage.bpmn
- elementId: offer
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage.offer.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage.offer.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 91. src/data/node-docs/feature-goal/mortgage.signing-advance.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage.bpmn
- elementId: signing-advance
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage.signing-advance.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage.signing-advance.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 92. src/data/node-docs/feature-goal/mortgage.signing.doc.ts

**NODE CONTEXT:**
- bpmnFile: mortgage.bpmn
- elementId: signing
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/mortgage.signing.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/mortgage.signing.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 93. src/data/node-docs/feature-goal/process-with-missing-subprocess.Call_Missing.doc.ts

**NODE CONTEXT:**
- bpmnFile: process-with-missing-subprocess.bpmn
- elementId: Call_Missing
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/process-with-missing-subprocess.Call_Missing.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/process-with-missing-subprocess.Call_Missing.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


### 94. src/data/node-docs/feature-goal/process-with-subprocess.Call_Sub.doc.ts

**NODE CONTEXT:**
- bpmnFile: process-with-subprocess.bpmn
- elementId: Call_Sub
- type: feature-goal

**Prompt att använda:**
- prompts/llm/feature_epic_prompt.md

**Fält som behöver uppdateras:**
- summary (TODO)
- testDescription (TODO)
- epics (empty array)
- scenarios (empty array)


**Instruktioner:**
1. Öppna filen: `src/data/node-docs/feature-goal/process-with-subprocess.Call_Sub.doc.ts`
2. **Läs hela filen FÖRST** och identifiera vilka fält som är 'TODO' vs vilka som redan har innehåll
3. Läs NODE CONTEXT-kommentaren överst i filen
4. Läs prompt-filen: prompts/llm/feature_epic_prompt.md
5. Generera JSON enligt promptens instruktioner
6. **Uppdatera BARA fälten som är 'TODO' eller tomma:** summary, testDescription, epics, scenarios
7. **LÄMNA ALLA ANDRA FÄLT ORÖRTA** - även om de inte är i listan ovan
8. **Uppdatera prompt-version kommentar:**
   - Om filen INTE har en PROMPT VERSION-kommentar → Lägg till en direkt efter NODE CONTEXT-kommentaren
   - Om filen HAR en PROMPT VERSION-kommentar → Uppdatera versionen till: 1.0.0
   - Format:
   ```typescript
   /**
    * PROMPT VERSION: 1.0.0
    * Genererad: 2025-11-26
    */
   ```
   Lägg till/uppdatera denna kommentar direkt efter NODE CONTEXT-kommentaren, INNAN export-satsen.
9. **Kontrollera INNAN du sparar:** Har du ändrat något som INTE var 'TODO'? → Ångra ändringen!
10. Spara filen
11. **Uppdatera statusfilen** (`.codex-batch-status.json`):
    - Lägg till `src/data/node-docs/feature-goal/process-with-subprocess.Call_Sub.doc.ts` i `completed`-arrayen (om den inte redan finns)
    - Uppdatera `current` till nästa fil i listan (eller null om alla är klara)
    - Uppdatera `lastUpdated` till aktuellt datum/tid (ISO-format)
    - **Fråga INTE om du ska fortsätta - bara uppdatera och gå vidare till nästa fil!**

---


## Checklista för varje fil

Innan du sparar en fil, kontrollera:
- [ ] Har jag bara ändrat fält som var 'TODO', [], eller ''?
- [ ] Har jag lämnat alla fält med befintligt innehåll orörda?
- [ ] Har jag inte tagit bort eller ändrat något innehåll som redan fanns?
- [ ] Har jag bara LAGT TILL innehåll i TODO-fält, inte ändrat befintligt?

**Om någon checklista är fel → Ångra ändringarna innan du sparar!**

## Automatisk bearbetning

**VIKTIGT:** Bearbeta ALLA 94 filer automatiskt!
- Starta med fil 1 och fortsätt till fil 94
- Fråga INTE om du ska fortsätta - bara fortsätt
- När en fil är klar, gå direkt vidare till nästa
- Stoppa bara om du stöter på ett faktiskt fel som du inte kan lösa

## Tips

- Bearbeta filerna kontinuerligt utan pauser
- Om du stöter på ett problem med en specifik fil, hoppa över den och fortsätt med nästa
- Kontrollera resultatet med `git diff src/data/node-docs/` när alla filer är klara
